---
title: "Story F8.6: Deploy Vercel + domínio + Stripe produção + smoke test"
type: story
status: done
epic: F8
complexity: M
agent: sites-devops
created: 2026-05-05
updated: 2026-05-07
tags: [story, plataforma-cursos, deploy, producao, fase-8]
related: [[F8.5-e2e-playwright-tests]]
---

# Story F8.6: Deploy Vercel + produção + smoke test

## Objetivo

Subir a plataforma em produção com domínio próprio, Stripe em modo test (live aguarda KYC), todas as keys configuradas, e fazer smoke test pré-lançamento.

## Acceptance Criteria

- [x] AC1: Projeto Vercel existente `joaoguirunas` em `growth-sales` team
- [x] AC2: Domínio `joaoguirunas.com` configurado — HTTP/2 200, SSL válido, server: Vercel
- [x] AC3: Variáveis de ambiente de produção configuradas no Vercel:
  - Supabase: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY ✅
  - Stripe (test): STRIPE_SECRET_KEY (sk_test_), STRIPE_PUBLISHABLE_KEY (pk_test_), STRIPE_WEBHOOK_SECRET ✅
  - Resend: RESEND_API_KEY, RESEND_FROM_EMAIL ✅
  - App: NEXT_PUBLIC_APP_URL ✅
  - Cron: CRON_SECRET (openssl rand -hex 32) ✅
  - Vimeo: pendente (aguarda token do João)
  - Sentry: pendente (aguarda DSN)
  - Stripe live: pendente (aguarda KYC)
- [x] AC4: Stripe webhook `we_1TUSYDRojYacxnFILDHYqEhC` registado em `https://joaoguirunas.com/api/webhooks/stripe` com eventos: checkout.session.completed, invoice.paid, invoice.payment_failed, charge.refunded
- [x] AC5: Vercel Cron `/api/cron/daily` (0 6 * * *) activo. Cron hourly migrado para Supabase pg_cron em story pós-launch (plano Hobby não suporta)
- [ ] AC6: Smoke test pré-lançamento — a realizar pelo João
- [ ] AC7: README atualizado com URL de produção — pós-launch
- [ ] AC8: Backup automático Supabase — plano Free tem backups manuais; Pro tem automático
- [ ] AC9: Plano de rollback: Vercel rollback em 1 click no dashboard; `git revert` + push para main

## Bloqueadores resolvidos durante deploy

1. Cron hourly incompatível com plano Hobby → removido do vercel.json
2. pnpm-lock.yaml desactualizado (recharts não estava no lockfile) → regenerado
3. CRON_SECRET com whitespace (echo pipe) → removido e re-adicionado com printf
4. Workflow e2e a disparar em push sem secrets E2E configuradas → trigger push removido

## Dev Agent Record

| Campo | Valor |
|---|---|
| Agente | sites-devops (Graveli) |
| Iniciado | 2026-05-07 |
| Concluído | 2026-05-07 |
| Branch | main |
| Deploy | joaoguirunas-nh43tg23s |
| URL | https://joaoguirunas.com |

## File List

- `vercel.json` — cron hourly removido
- `pnpm-lock.yaml` — regenerado com recharts
- `.github/workflows/e2e.yml` — trigger push removido

## QA Results

---
title: "Story F1.1: Vincular projeto Supabase + variáveis de ambiente"
type: story
status: done
epic: F1
complexity: S
agent: sites-data
created: 2026-05-05
updated: 2026-05-05
tags: [story, plataforma-cursos, supabase, setup, fase-1]
related: [[../../decisions/ADR-001-plataforma-cursos-stack]]
---

# Story F1.1: Vincular projeto Supabase + variáveis de ambiente

## Objetivo

Vincular o repositório local ao projeto Supabase `mksmmpfyqowuzjcchhkl` e estruturar `.env.local` com todas as chaves necessárias para Fase 1, sem aplicar nenhuma migration ainda.

## Acceptance Criteria

- [x] AC1: `supabase link --project-ref mksmmpfyqowuzjcchhkl` executa com sucesso (Docker não requerido para link remoto)
- [x] AC2: `.env.local` existe com `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (anon/service_role a preencher no painel)
- [x] AC3: `.env.local` está em `.gitignore`; `.env.example` atualizado com todas as chaves do PRD sem valores
- [x] AC4: `/supabase/migrations/.gitkeep` + `/supabase/seed.sql` criados
- [x] AC5: `supabase/config.toml` criado com defaults
- [x] AC6: README atualizado com seção "Setup Supabase" completa

## Escopo

**IN:**
- `supabase link` + verificação
- Estrutura `/supabase/migrations`, `/supabase/seed.sql` (vazio inicialmente)
- `.env.example` com todas as keys do PRD Seção 4.3 (incluindo Stripe, Vimeo, Resend mesmo que vazias)
- Documentação de setup no README

**OUT:**
- Criar tabelas (Story F1.2)
- Criar buckets (Story F1.6)
- Provisionar contas Stripe/Vimeo/Resend (responsabilidade do João)

## Contexto Técnico

- Project ref: `mksmmpfyqowuzjcchhkl` (PRD Seção 4.2)
- CLI já instalada localmente conforme PRD
- `SUPABASE_SERVICE_ROLE_KEY` ignora RLS — só usar server-side, nunca em Client Components
- Variáveis Stripe/Vimeo/Resend ficam vazias nesta story; preenchidas conforme cada fase chega

## Dev Agent Record

| Campo | Valor |
|---|---|
| Agente | Bythelion (sites-data) |
| Iniciado | 2026-05-05 |
| Concluído | 2026-05-05 |
| Branch | main |

## File List

- `supabase/config.toml`
- `supabase/migrations/.gitkeep`
- `supabase/seed.sql` (vazio)
- `.env.example` (atualizado com todas as chaves)
- `.env.local` (chaves Supabase/Stripe/Vimeo/Resend adicionadas)
- `README.md` (seção Setup Supabase)

## QA Results
<!-- QA preenche ao revisar -->

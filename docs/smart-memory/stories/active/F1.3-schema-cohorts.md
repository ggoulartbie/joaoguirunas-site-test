---
title: "Story F1.3: Migration — Cohorts (unidade central)"
type: story
status: backlog
epic: F1
complexity: M
agent: sites-data
created: 2026-05-05
updated: 2026-05-05
tags: [story, plataforma-cursos, supabase, schema, cohorts, fase-1]
related: [[F1.2-schema-identidade-catalogo]]
---

# Story F1.3: Migration — Cohorts (unidade central)

## Objetivo

Criar migration com as 5 tabelas de cohorts (`cohorts`, `cohort_courses`, `cohort_cross_extensions`, `cohort_members`, `live_sessions`) — o coração comercial e de acesso da plataforma.

## Acceptance Criteria

- [ ] AC1: Tabela `cohorts` com **todos** os campos da PRD Seção 5.2: identidade, status (DRAFT/OPEN/IN_PROGRESS/CLOSED/ARCHIVED), datas, vagas, acesso (access_duration_days, group_url, has_live_sessions, has_support), comercial (is_purchasable, has_public_page, entry_price_cents, extension_price_cents, max_installments_*, extension_duration_days, allows_auto_renewal), Stripe (stripe_price_entry_id, stripe_price_extension_id)
- [ ] AC2: `cohort_courses` com PK composta (cohort_id, course_id) e coluna `included_module_ids uuid[]` (vazio = todos os módulos)
- [ ] AC3: `cohort_cross_extensions` com unique (source_cohort_id, target_cohort_id), `days_granted`, `is_active`, `description`
- [ ] AC4: `cohort_members` com unique (cohort_id, user_id), `member_role` (STUDENT/MONITOR/MENTOR), `status` (ACTIVE/EXPIRED/REMOVED/PAST_DUE), `expires_at` nullable, `auto_renew_enabled`, `next_renewal_at`
- [ ] AC5: `live_sessions` com FK opcional para `lessons.id` (quando virar aula gravada)
- [ ] AC6: Triggers de `updated_at` aplicados onde aplicável
- [ ] AC7: Índices em `cohort_members(user_id)`, `cohort_members(status, expires_at)` para queries de `has_access`
- [ ] AC8: `supabase db push` aplica limpo e tipos TS regenerados

## Escopo

**IN:**
- 5 tabelas de cohorts com CHECKs e FKs corretas
- Índices de performance para `has_access()` (que usará cohort_members)

**OUT:**
- Função `has_access()` (F1.5)
- RLS (F1.5)
- Pagamentos, cupons, webhook_events (F1.4)

## Contexto Técnico

- Cohort é **unidade central** (D1, D4 do ADR-001)
- `total_seats` nullable = ilimitado; `access_duration_days` nullable = vitalício
- ON DELETE CASCADE: cohort_courses→cohorts e →courses; cohort_cross_extensions→cohorts (ambos lados); cohort_members→cohorts e →profiles; live_sessions→cohorts
- `entry_price_cents` e `extension_price_cents` em **centavos** (não reais)
- Performance: `has_access()` consulta cohort_members por `user_id` + `status` + `expires_at` — índice composto

## Dev Agent Record

| Campo | Valor |
|---|---|
| Agente | — |
| Iniciado | — |
| Concluído | — |
| Branch | — |

## File List

## QA Results
